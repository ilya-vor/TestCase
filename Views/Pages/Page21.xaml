<Page
    x:Class="TestCase.Views.Pages.Page21"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:TestCase.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Page21"
    d:DataContext="{d:DesignInstance local:Page21,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>


        <TextBlock
            Grid.Row="0"
            Margin="0,12"
            VerticalAlignment="Center"
            Text="Написать программу на языке C#, которая запускает xslt-преобразование, написанное в задании I." />

        <ui:Button
            Grid.Row="1"
            Command="{Binding ViewModel.RunXsltTransformationCommand, Mode=OneWay}"
            VerticalAlignment="Top"
            Content="Запустить Xslt преобразование"
            Icon="Fluent24" />

        <Grid Grid.Row="2">
            <TabControl Margin="10">
                <TabItem Header="Таблица">
                    <DataGrid x:Name="employeesGrid" AutoGenerateColumns="False" 
                         ItemsSource="{Binding ViewModel.EmployeesData.EmployeeList}" 
                         CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="120"/>
                            <DataGridTextColumn Header="Фамилия" Binding="{Binding Surname}" Width="120"/>
                            <!--<DataGridTextColumn Header="Всего зарплат" Binding="{Binding TotalSalary}" Width="100"/>-->
                            <DataGridTemplateColumn Header="Зарплаты по месяцам" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ItemsControl ItemsSource="{Binding Salaries}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Margin="0,2">
                                                        <TextBlock Text="{Binding Month}" FontWeight="Bold" 
                                                              Margin="0,0,5,0"/>
                                                        <TextBlock Text="{Binding Amount, StringFormat={}{0:C}}"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>

                <TabItem Header="Дерево">
                    <TreeView x:Name="employeesTree" Margin="5" ItemsSource="{Binding ViewModel.EmployeesData.EmployeeList}">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Salaries}">
                                <TextBlock Text="{Binding FullName}" FontWeight="Bold"/>
                                <HierarchicalDataTemplate.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Month}" Margin="0,0,10,0"/>
                                            <TextBlock Text="{Binding Amount, StringFormat={}{0:C}}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </HierarchicalDataTemplate.ItemTemplate>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
    
</Page>
