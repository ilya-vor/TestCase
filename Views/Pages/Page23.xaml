<Page
    x:Class="TestCase.Views.Pages.Page23"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:TestCase.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Page23"
    d:DataContext="{d:DesignInstance local:Page23,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>


        <TextBlock
            Grid.Row="0"
            VerticalAlignment="Center"
            TextWrapping="Wrap"
            Margin="12,12"
            Text="Написать программу на языке C#, которая в исходный файл Data1.xml в элемент Pay дописывает атрибут, который отражает сумму всех amount" />

        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <ui:Button
            Command="{Binding ViewModel.ButtonClickedCommand, Mode=OneWay}"
            VerticalAlignment="Top"
            Content="Открыть обновленный Data1.xml"
            Margin="12,0"
            Icon="Fluent24"/>
        </StackPanel>
    </Grid>
    
</Page>
